num_rows = 60
num_cols = 60
num_layers = 10 # of each outline of the frame
num_frames = 2
num_vertical_rows = 0.1 # number of rows for the vertical window claculatin (velocity analysis)
                        # should be a float betwenn [0,1] - if 1: all rows are taken 
                        # or "None" for the default value
neurons_out = True